<!-- Created with Jaspersoft Studio version 7.0.1.final using JasperReports Library version 7.0.1-573496633c2b4074e32f433154b543003f7d2498  -->
<jasperReport name="Income_Statement_Jasper_Report" language="java" pageWidth="595" pageHeight="842" columnWidth="555" leftMargin="20" rightMargin="20" topMargin="20" bottomMargin="20" uuid="f8545f43-2697-44d4-b33a-9a7826c5b4a0">
	<property name="com.jaspersoft.studio.data.sql.tables" value=""/>
	<property name="com.jaspersoft.studio.data.defaultdataadapter" value="mifos"/>
	<style name="Table_TH" mode="Opaque" backcolor="#F0F8FF">
		<box>
			<pen lineWidth="0.5" lineColor="#000000"/>
			<topPen lineWidth="0.5" lineColor="#000000"/>
			<leftPen lineWidth="0.5" lineColor="#000000"/>
			<bottomPen lineWidth="0.5" lineColor="#000000"/>
			<rightPen lineWidth="0.5" lineColor="#000000"/>
		</box>
	</style>
	<style name="Table_CH" mode="Opaque" backcolor="#BFE1FF">
		<box>
			<pen lineWidth="0.5" lineColor="#000000"/>
			<topPen lineWidth="0.5" lineColor="#000000"/>
			<leftPen lineWidth="0.5" lineColor="#000000"/>
			<bottomPen lineWidth="0.5" lineColor="#000000"/>
			<rightPen lineWidth="0.5" lineColor="#000000"/>
		</box>
	</style>
	<style name="Table_TD" mode="Opaque" backcolor="#FFFFFF">
		<box>
			<pen lineWidth="0.5" lineColor="#000000"/>
			<topPen lineWidth="0.5" lineColor="#000000"/>
			<leftPen lineWidth="0.5" lineColor="#000000"/>
			<bottomPen lineWidth="0.5" lineColor="#000000"/>
			<rightPen lineWidth="0.5" lineColor="#000000"/>
		</box>
	</style>
	<dataset name="Dataset" uuid="6fc3efee-0db2-4b8c-acf5-4b623923a16d">
		<property name="com.jaspersoft.studio.data.defaultdataadapter" value="mifos"/>
		<property name="com.jaspersoft.studio.data.sql.tables" value=""/>
		<parameter name="startDate" class="java.sql.Date"/>
		<parameter name="endDate" class="java.sql.Date"/>
		<parameter name="officeId" class="java.lang.Integer"/>
		<query language="sql"><![CDATA[SELECT * FROM (
    SELECT debits.glcode AS glcode,
           debits.name AS name,
           'Expense' AS IncomeOrExpense,
           (IFNULL(debits.debitamount, 0) - IFNULL(credits.creditamount, 0)) AS balance
    FROM (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS debitamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 2
          AND acc_gl_account.classification_enum IN (5)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) debits
    LEFT OUTER JOIN (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS creditamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 1
          AND acc_gl_account.classification_enum IN (5)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) credits ON debits.glcode = credits.glcode

    UNION

    SELECT credits.glcode AS glcode,
           credits.name AS name,
           'Expense' AS IncomeOrExpense,
           (IFNULL(debits.debitamount, 0) - IFNULL(credits.creditamount, 0)) AS balance
    FROM (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS debitamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 2
          AND acc_gl_account.classification_enum IN (5)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) debits
    RIGHT OUTER JOIN (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS creditamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 1
          AND acc_gl_account.classification_enum IN (5)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) credits ON debits.glcode = credits.glcode
) AS fullouterjoinresult_expense

UNION

SELECT * FROM (
    SELECT debits.glcode AS glcode,
           debits.name AS name,
           'Income' AS IncomeOrExpense,
           (IFNULL(credits.creditamount, 0) - IFNULL(debits.debitamount, 0)) AS balance
    FROM (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS debitamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 2
          AND acc_gl_account.classification_enum IN (4)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) debits
    LEFT OUTER JOIN (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS creditamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 1
          AND acc_gl_account.classification_enum IN (4)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) credits ON debits.glcode = credits.glcode

    UNION

    SELECT credits.glcode AS glcode,
           credits.name AS name,
           'Income' AS IncomeOrExpense,
           (IFNULL(credits.creditamount, 0) - IFNULL(debits.debitamount, 0)) AS balance
    FROM (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS debitamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 2
          AND acc_gl_account.classification_enum IN (4)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) debits
    RIGHT OUTER JOIN (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS creditamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 1
          AND acc_gl_account.classification_enum IN (4)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) credits ON debits.glcode = credits.glcode
) AS fullouterjoinresult_income
ORDER BY glcode;
]]></query>
		<field name="glcode" class="java.lang.String">
			<property name="com.jaspersoft.studio.field.name" value="glcode"/>
			<property name="com.jaspersoft.studio.field.label" value="glcode"/>
		</field>
		<field name="name" class="java.lang.String">
			<property name="com.jaspersoft.studio.field.name" value="name"/>
			<property name="com.jaspersoft.studio.field.label" value="name"/>
		</field>
		<field name="IncomeOrExpense" class="java.lang.String">
			<property name="com.jaspersoft.studio.field.name" value="IncomeOrExpense"/>
			<property name="com.jaspersoft.studio.field.label" value="IncomeOrExpense"/>
		</field>
		<field name="balance" class="java.math.BigDecimal">
			<property name="com.jaspersoft.studio.field.name" value="balance"/>
			<property name="com.jaspersoft.studio.field.label" value="balance"/>
		</field>
	</dataset>
	<parameter name="startDate" class="java.sql.Date"/>
	<parameter name="endDate" class="java.sql.Date"/>
	<parameter name="officeId" class="java.lang.Integer"/>
	<query language="SQL"><![CDATA[SELECT * FROM (
    SELECT debits.glcode AS glcode,
           debits.name AS name,
           'Expense' AS IncomeOrExpense,
           (IFNULL(debits.debitamount, 0) - IFNULL(credits.creditamount, 0)) AS balance
    FROM (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS debitamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 2
          AND acc_gl_account.classification_enum IN (5)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) debits
    LEFT OUTER JOIN (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS creditamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 1
          AND acc_gl_account.classification_enum IN (5)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) credits ON debits.glcode = credits.glcode

    UNION

    SELECT credits.glcode AS glcode,
           credits.name AS name,
           'Expense' AS IncomeOrExpense,
           (IFNULL(debits.debitamount, 0) - IFNULL(credits.creditamount, 0)) AS balance
    FROM (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS debitamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 2
          AND acc_gl_account.classification_enum IN (5)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) debits
    RIGHT OUTER JOIN (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS creditamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 1
          AND acc_gl_account.classification_enum IN (5)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) credits ON debits.glcode = credits.glcode
) AS fullouterjoinresult_expense

UNION

SELECT * FROM (
    SELECT debits.glcode AS glcode,
           debits.name AS name,
           'Income' AS IncomeOrExpense,
           (IFNULL(credits.creditamount, 0) - IFNULL(debits.debitamount, 0)) AS balance
    FROM (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS debitamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 2
          AND acc_gl_account.classification_enum IN (4)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) debits
    LEFT OUTER JOIN (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS creditamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 1
          AND acc_gl_account.classification_enum IN (4)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) credits ON debits.glcode = credits.glcode

    UNION

    SELECT credits.glcode AS glcode,
           credits.name AS name,
           'Income' AS IncomeOrExpense,
           (IFNULL(credits.creditamount, 0) - IFNULL(debits.debitamount, 0)) AS balance
    FROM (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS debitamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 2
          AND acc_gl_account.classification_enum IN (4)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) debits
    RIGHT OUTER JOIN (
        SELECT acc_gl_account.gl_code AS glcode,
               name,
               SUM(amount) AS creditamount
        FROM acc_gl_journal_entry, acc_gl_account
        WHERE acc_gl_account.id = acc_gl_journal_entry.account_id
          AND acc_gl_journal_entry.type_enum = 1
          AND acc_gl_account.classification_enum IN (4)
          AND acc_gl_journal_entry.entry_date BETWEEN $P{startDate} AND $P{endDate}
          AND (acc_gl_journal_entry.office_id = $P{officeId} OR $P{officeId} = 1)
        GROUP BY gl_code
    ) credits ON debits.glcode = credits.glcode
) AS fullouterjoinresult_income
ORDER BY glcode;
]]></query>
	<field name="glcode" class="java.lang.String">
		<property name="com.jaspersoft.studio.field.name" value="glcode"/>
		<property name="com.jaspersoft.studio.field.label" value="glcode"/>
	</field>
	<field name="name" class="java.lang.String">
		<property name="com.jaspersoft.studio.field.name" value="name"/>
		<property name="com.jaspersoft.studio.field.label" value="name"/>
	</field>
	<field name="IncomeOrExpense" class="java.lang.String">
		<property name="com.jaspersoft.studio.field.name" value="IncomeOrExpense"/>
		<property name="com.jaspersoft.studio.field.label" value="IncomeOrExpense"/>
	</field>
	<field name="balance" class="java.math.BigDecimal">
		<property name="com.jaspersoft.studio.field.name" value="balance"/>
		<property name="com.jaspersoft.studio.field.label" value="balance"/>
	</field>
	<background splitType="Stretch"/>
	<summary height="438" splitType="Stretch">
		<element kind="component" uuid="d8c67894-598a-465d-b9d9-b7ccbce56102" x="0" y="80" width="410" height="150">
			<component kind="table">
				<datasetRun uuid="809868da-7c46-4eea-8fd4-b4c325a7bf0a" subDataset="Dataset">
					<connectionExpression><![CDATA[$P{REPORT_CONNECTION}]]></connectionExpression>
					<parameter name="startDate">
						<expression><![CDATA[${startDate}]]></expression>
					</parameter>
					<parameter name="endDate">
						<expression><![CDATA[${endDate}]]></expression>
					</parameter>
					<parameter name="officeId">
						<expression><![CDATA[${officeId}]]></expression>
					</parameter>
				</datasetRun>
				<column kind="single" uuid="4238667e-34d9-49f7-8b58-119710265d10" width="100">
					<tableHeader height="30" style="Table_TH"/>
					<tableFooter height="30" style="Table_TH"/>
					<columnHeader height="30" style="Table_CH">
						<element kind="staticText" uuid="ce81eca5-31ec-4f49-8ba2-d2df00d56403" x="0" y="0" width="100" height="30" bold="true" hTextAlign="Center">
							<text><![CDATA[glcode]]></text>
						</element>
					</columnHeader>
					<columnFooter height="30" style="Table_CH"/>
					<detailCell height="30" style="Table_TD">
						<element kind="textField" uuid="f2e31a0a-f2ce-4e70-b74e-22ffcaafe9bd" x="0" y="0" width="100" height="30" bold="false" hTextAlign="Center">
							<expression><![CDATA[$F{glcode}]]></expression>
						</element>
					</detailCell>
				</column>
				<column kind="single" uuid="b5df2edf-e89c-4e9c-b5aa-575fb338af72" width="100">
					<tableHeader height="30" style="Table_TH"/>
					<tableFooter height="30" style="Table_TH"/>
					<columnHeader height="30" style="Table_CH">
						<element kind="staticText" uuid="cedd4334-fe3c-48a3-8eee-8d3bd13b536a" x="0" y="0" width="100" height="30" bold="true" hTextAlign="Center">
							<text><![CDATA[name]]></text>
						</element>
					</columnHeader>
					<columnFooter height="30" style="Table_CH"/>
					<detailCell height="30" style="Table_TD">
						<element kind="textField" uuid="7c39b6c9-3fdf-43e8-8337-30dba7260da4" x="0" y="0" width="100" height="30" bold="false" hTextAlign="Center">
							<expression><![CDATA[$F{name}]]></expression>
						</element>
					</detailCell>
				</column>
				<column kind="single" uuid="48960e82-b19a-4612-a6ab-5744422f49e9" width="100">
					<tableHeader height="30" style="Table_TH"/>
					<tableFooter height="30" style="Table_TH"/>
					<columnHeader height="30" style="Table_CH">
						<element kind="staticText" uuid="9124a92e-f092-4dd0-9fcd-4258c9096661" x="0" y="0" width="100" height="30" bold="true" hTextAlign="Center">
							<text><![CDATA[IncomeOrExpense]]></text>
						</element>
					</columnHeader>
					<columnFooter height="30" style="Table_CH"/>
					<detailCell height="30" style="Table_TD">
						<element kind="textField" uuid="a79e95e5-2aee-4e96-9ddb-34cdb16bf28d" x="0" y="0" width="100" height="30" bold="false" hTextAlign="Center">
							<expression><![CDATA[$F{IncomeOrExpense}]]></expression>
						</element>
					</detailCell>
				</column>
				<column kind="single" uuid="5208c366-511f-47fa-83d6-3837f7c37ca0" width="110">
					<tableHeader height="30" style="Table_TH"/>
					<tableFooter height="30" style="Table_TH"/>
					<columnHeader height="30" style="Table_CH">
						<element kind="staticText" uuid="51a50846-89e6-4bc9-8880-8938543f8cea" x="0" y="0" width="110" height="30" bold="true" hTextAlign="Center">
							<text><![CDATA[balance]]></text>
						</element>
					</columnHeader>
					<columnFooter height="30" style="Table_CH"/>
					<detailCell height="30" style="Table_TD">
						<element kind="textField" uuid="e09c7905-dbac-45e8-851f-4642f66dd7a9" x="0" y="0" width="110" height="30" bold="false" hTextAlign="Center">
							<expression><![CDATA[$F{balance}]]></expression>
						</element>
					</detailCell>
				</column>
			</component>
			<property name="com.jaspersoft.studio.layout" value="com.jaspersoft.studio.editor.layout.VerticalRowLayout"/>
			<property name="com.jaspersoft.studio.table.style.table_header" value="Table_TH"/>
			<property name="com.jaspersoft.studio.table.style.column_header" value="Table_CH"/>
			<property name="com.jaspersoft.studio.table.style.detail" value="Table_TD"/>
		</element>
	</summary>
</jasperReport>
